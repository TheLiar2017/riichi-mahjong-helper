# 立直麻将助手 (Riichi Mahjong Helper)

日式立直麻将规则查询、役种详解、符数点数计算工具。

## 功能

- 基础规则查询（场风自风、食断、振听等）
- 40+ 役种详解（含手牌示例、特殊规则、常见陷阱）
- 图形化符数与点数计算器
- **役种自动检测** — 输入手牌，自动识别可胡牌型

## 最近更新

- `2026-04` — 役种自动检测功能上线，可根据手牌智能识别可胡牌型
- `2026-04` — 修复符数计算问题，完善点数计算逻辑
- `2026-04` — 计算器 UI 优化：副露分区、同牌多选、赤宝牌支持

## 待办事项

- [ ] 优化计算器番数计算（役种累计、宝牌加成）
- [ ] 优化计算器符数计算（面子组成、雀头、听牌形状）

## 技术栈

## 技术栈

- **Core**: TypeScript（核心逻辑，可跨平台复用）
- **Web**: Vue 3 + Vite + TypeScript + Vue Router
- **样式**: 日式和风主题

## 开发

```bash
npm install
npm run dev      # 启动开发服务器 (packages/web)
npm run build    # 构建生产版本
npm run preview  # 预览构建结果
```

## 架构

```
packages/
├── core/                    # 核心逻辑（牌数据、役种、符数点数计算）
│   └── src/
│       ├── data/            # 牌数据、役种数据、规则数据
│       ├── domain/          # 符数计算、点数计算、役种检测等业务逻辑
│       └── index.ts         # 统一导出
└── web/                     # Vue 3 前端应用
    └── src/
        ├── components/      # UI 组件
        ├── views/           # 页面视图
        ├── styles/          # 日式和风主题样式
        └── router/          # Vue Router 配置
```

核心逻辑与 UI 完全分离，`packages/core` 可独立使用，可复用开发 CLI、小程序等。
